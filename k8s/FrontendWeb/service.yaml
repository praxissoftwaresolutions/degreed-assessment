apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: web
spec:
  type: LoadBalancer # Creates a Public IP for your demo
  selector:
    app: web
  ports:
  - port: 80
    targetPort: 8080