apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: api
spec:
  type: LoadBalancer # Changed from ClusterIP for external access
  selector:
    app: api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080 # Matches EXPOSE 8080 in your Dockerfile
